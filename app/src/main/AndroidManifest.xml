<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pamguiaseries">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <uses-feature
        android:name="android.hardware.accelerometer"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/pipoca"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PAMGuiaSeries">
        <activity android:name=".VisualizarComments"></activity>
        <activity android:name=".CommentsPro" />
        <activity android:name=".seriesVistas" />
        <activity android:name=".lojas" />
        <activity android:name=".perfilUser" />
        <activity android:name=".ActivityMonster" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Eventos" />
        <activity android:name=".WandaVision" />
        <activity android:name=".cadastro">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.SEARCH" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>